fun filter(xs test) {
  let res := []
  each item in xs {
    if test(item) { push(res item) }
  }
  res
}

fun get-evens(min max) {
  let list := []
  each i in min..max { push(list i) }
  filter(list fun _(i) { mod(i 2) = 0 })
}

each i in :helloworld {
  print(i)
}
